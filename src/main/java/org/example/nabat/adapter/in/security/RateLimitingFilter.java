package org.example.nabat.adapter.in.security;

// Rate limiting removed — will be handled at the API-gateway layer.
// Keeping this file as an empty placeholder to avoid a dead-class warning
// until the package is cleaned up in a follow-up commit.
